import React from 'react';
import { BuyerWorkspace } from '../../components/BuyerWorkspace';

export const BuyerProfilePage: React.FC = () => {
  return (
    <BuyerWorkspace
      title="Buyer Profile"
      description="Update your personal details, communication preferences, and account settings."
    />
  );
};
